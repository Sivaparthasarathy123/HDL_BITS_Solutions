module full_adder(
    input x,y,
    output sum);
    
    assign sum = x^y;
endmodule

module top_module (
    input [3:0] x,
    input [3:0] y, 
    output [4:0] sum);
    
    assign sum = x+y;
endmodule
